---
name: feature-start
description: Create a new feature branch as a git worktree following the playbook's strict branching rules.
argument-hint: "<prefix/branch-name>"
---

Read and follow `skills/feature-start/SKILL.md` (path relative to the repository root; this repository
is the playbook, so the file is the skill itself, not a copy). Arguments: $ARGUMENTS

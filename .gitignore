# Feature worktrees ({WORKTREE_DIR} per rules/git-worktree-workflow.md).
# These are checkouts OF this repo, never content of it — following the repo's own
# branching rule must not leave a nested working tree staged for commit.
.ai/worktrees/

# Written by hooks/git/post-merge for the next session to read. Per-machine state
# (it names local worktrees and undeleted branches), regenerated on every merge.
# .claude/settings.json IS tracked — ignore the artifact, not the directory.
.claude/post-merge-note.md

# macOS finder metadata
.DS_Store
